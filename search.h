#ifndef SEARCH_H
#define SEARCH_H

#include <stdio.h>
#include <stdlib.h>
#include <strings.h>
#include <ctype.h>
#include "structure.h"

//book ** search(Array *books, char* searchString);
void search(Array *books, foundBooks *fbo, char* searchString);

#endif
